name: Admin Portal CI/CD

on:
  push:
    branches: [main]
    paths:
      - 'admin-portal/**'
      - '.github/workflows/admin-portal.yml'
    tags:
      - 'v*'
  pull_request:
    branches: [main]
    paths:
      - 'admin-portal/**'

defaults:
  run:
    working-directory: ./admin-portal

jobs:
  lint-and-typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: admin-portal/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run type-check

  build:
    needs: lint-and-typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: admin-portal/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build
        env:
          NEXT_PUBLIC_API_BASE_URL: ${{ secrets.ADMIN_API_BASE_URL || 'https://api.tribewatch.io' }}
          NEXT_PUBLIC_COGNITO_REGION: eu-west-2
          NEXT_PUBLIC_COGNITO_USER_POOL_ID: ${{ secrets.COGNITO_ADMIN_USER_POOL_ID }}
          NEXT_PUBLIC_COGNITO_APP_CLIENT_ID: ${{ secrets.COGNITO_ADMIN_APP_CLIENT_ID }}
        run: npm run build

  deploy-dev:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: dev
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-west-2

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: admin-portal/package-lock.json

      - name: Install and build for dev
        env:
          NEXT_PUBLIC_API_BASE_URL: https://api-dev.tribewatch.io
          NEXT_PUBLIC_COGNITO_REGION: eu-west-2
          NEXT_PUBLIC_COGNITO_USER_POOL_ID: ${{ secrets.COGNITO_ADMIN_USER_POOL_ID_DEV }}
          NEXT_PUBLIC_COGNITO_APP_CLIENT_ID: ${{ secrets.COGNITO_ADMIN_APP_CLIENT_ID_DEV }}
        run: |
          npm ci
          npm run build

      - name: Deploy to S3 + CloudFront (dev)
        env:
          S3_BUCKET: ${{ secrets.ADMIN_S3_BUCKET_DEV }}
          CF_DISTRIBUTION: ${{ secrets.ADMIN_CF_DISTRIBUTION_DEV }}
        run: |
          aws s3 sync .next/static s3://$S3_BUCKET/_next/static --delete
          aws s3 sync public s3://$S3_BUCKET --delete
          aws cloudfront create-invalidation --distribution-id $CF_DISTRIBUTION --paths "/*"
