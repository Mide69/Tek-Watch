/**
 * useRegionFilter — returns the currently selected region from RegionContext.
 * Use this in page components to pass the region to API calls.
 */
export { useRegion as useRegionFilter } from '@/contexts/RegionContext'
